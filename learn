fmt
fmt.Print(x)                  // выводит x на экран как есть без переносов
fmt.Println(x)                // выводит x и переносит строку в конце
fmt.Printf("..", x)           // выводит текст по шаблону (например, %.2f)
fmt.Scan(&x)                  // ждет ввод текста от пользователя в переменную x
fmt.Sprintf("..", x)          // записывает отформатированный текст в переменную вместо вывода

strings
strings.Contains(str, x)      // ищет слово x внутри строки str (true/false)
strings.ToLower(str)          // делает все буквы в тексте маленькими
strings.ToUpper(str)          // делает все буквы в тексте ЗАГЛАВНЫМИ
strings.ReplaceAll(str, a, b) // заменяет все старые буквы a на новые b
strings.Split(str, x)         // режет одну строку на массив строк по разделителю x
strings.Join(array, x)        // склеивает массив строк в одну строку через разделитель x
strings.HasPrefix(str, x)     // проверяет, начинается ли строка str на текст x (true/false)
strings.HasSuffix(str, x)     // проверяет, заканчивается ли строка str на текст x (true/false)
strings.TrimSpace(str)        // удаляет все лишние пробелы в начале и конце строки

strconv
strconv.Atoi(str)             // превращает текст "123" в число 123
strconv.Itoa(num)             // превращает число 123 в текст "123"
strconv.ParseFloat(str, 64)   // превращает текст "12.34" в дробное число float64

time
time.Now()                    // выдает текущее время компьютера прямо сейчас
time.Sleep(2 * time.Second)    // ставит программу на паузу на 2 секунды
time.Since(start)             // считает сколько времени прошло с точки start
time.Hour                     // константа одного часа времени для расчетов
time.Minute                   // константа одной минуты времени для расчетов
time.Millisecond              // константа одной миллисекунды времени для расчетов

math
math.Max(a, b)                // находит большее из двух дробных чисел
math.Min(a, b)                // находит меньшее из двух дробных чисел
math.Abs(x)                   // убирает минус у числа (делает положительным)
math.Round(x)                 // округляет дробь до ближайшего целого числа
math.Ceil(x)                  // округляет дробь всегда в большую сторону (вверх)
math.Floor(x)                 // округляет дробь всегда в меньшую сторону (вниз)
math.Pow(x, y)                // возводит число x в степень y

os
os.Create("file.txt")         // создает новый пустой файл на диске
os.Open("file.txt")           // открывает уже существующий файл для чтения
os.ReadFile("file.txt")       // читает весь файл целиком в память одной командой
os.WriteFile("f.txt", data, 0644) // записывает данные в файл (если файла нет, создаст его)
os.Remove("file.txt")         // навсегда удаляет файл или пустую папку с диска
os.Getwd()                    // выдает путь к папке, в которой сейчас запущена программа
os.Exit(1)                    // мгновенно закрывает программу с указанным кодом ошибки

sort
sort.Ints(array)              // сортирует массив целых чисел по возрастанию (от малых к большим)
sort.Strings(array)           // сортирует массив строк по алфавиту (от А до Я)
sort.Slice(array, функция)    // сортирует любой сложный массив по вашему правилу

math/rand

rand.Intn(x)                  // рандомное число от 0 до x-1
rand.Intn(x) + 1              // рандомное число от 1 до x
rand.Int31()                  // рандомное 32-битное положительное число
rand.Int63()                  // рандомное 64-битное положительное число
rand.Float64()                // рандомное дробное число от 0.0 до 1.0
rand.Perm(x)                  // перемешанный массив из чисел от 0 до x-1
rand.Shuffle(длина, способ)   // рандомная перестановка элементов внутри г

r
r.PathValue("name")           // достает слово из динамического пути вроде /user/{name} (Go 1.22+)
r.Method                      // выдает метод запроса строкой ЗАГЛАВНЫМИ буквами ("GET", "POST")
r.FormValue
r.URL.Path                    // выдает чистый адрес страницы, на которую зашел юзер ("/user/anton")
r.URL.Query().Get("age")      // достает значение параметра из ссылки после знака ?age=25
r.URL.Query().Has("age")      // проверяет, передан ли вообще параметр после знака ? (true/false)
r.Header.Get("User-Agent")    // читает скрытые технические данные о браузере или устройстве юзера
r.Header.Get("Authorization") // достает секретный токен или пароль, который прислал клиент
r.RemoteAddr                  // выдает IP-адрес и порт компьютера, с которого пришел запрос
r.ContentLength               // выдает размер присланных данных (размер файла или текста) в байтах
r.Cookie("session_id")        // находит и читает конкретную куку (cookie) по её имени
r.Cookies()                   // возвращает массив вообще всех кук, которые прислал браузер
r.Context()                   // дает доступ к контексту запроса (нужно для отмены тяжелых задач)
io.ReadAll(r.Body)            // читает весь присланный текст или JSON из тела POST-запроса

w
fmt.Fprint(w, "текст")        // записывает обычный текст или HTML-код прямо в ответ пользователю
fmt.Fprintf(w, "Привет, %s", x) // записывает отформированный текст с переменными в ответ пользователю
w.Write([]byte("текст"))      // базовый метод отправки текста в виде байт (fmt.Fprint делает это сам)
w.WriteHeader(200)            // задает HTTP-код успеха (пишется автоматически, если не вызвать руками)
w.WriteHeader(400)            // задает HTTP-код ошибки (Bad Request — клиент прислал чушь)
w.WriteHeader(401)            // задает HTTP-код ошибки (Unauthorized — юзер не ввел пароль)
w.WriteHeader(403)            // задает HTTP-код ошибки (Forbidden — юзеру сюда запрещено заходить)
w.WriteHeader(404)            // задает HTTP-код ошибки (Not Found — такой страницы или юзера нет)
w.WriteHeader(500)            // задает HTTP-код ошибки (Internal Server Error — сломался код на сервере)
w.Header().Set("K", "V")      // жестко заменяет заголовок, например ("Content-Type", "text/html")
w.Header().Add("K", "V")      // добавляет еще одно значение к существующему заголовку
w.Header().Del("K")           // полностью удаляет конкретный заголовок из ответа
http.SetCookie(w, cookie)     // отправляет и сохраняет куку (cookie) в браузере пользователя
http.Error(w, "Текст", 404)   // быстрая команда: сама делает WriteHeader(404) и пишет текст ошибки
http.Redirect(w, r, "/url", 302) // мгновенно перенаправляет (редиректит) пользователя на другую страницу

http/templates

template.ParseFiles("file.html")    // загружает и парсит конкретный файл шаблона
template.ParseGlob("t/*.html")      // загружает все файлы по маске из папки
template.Must(t, err)               // «защита»: если при парсинге ошибка, программа упадет (для критичных шаблонов)
tmpl.Execute(w, data)               // вставляет структуру data в шаблон и отправляет пользователю
tmpl.ExecuteTemplate(w, "name", d)  // выполняет конкретный именованный блок внутри шаблона

{{.Field}}                          // выводит значение поля из структуры (например, .Title)
{{if .IsActive}} ... {{end}}        // условие: если true, то показывает блок
{{range .Items}} ... {{end}}        // цикл: перебирает массив или слайс Items
{{with .User}} {{.Name}} {{end}}    // «заходит» внутрь объекта: внутри обращение идет через .
{{template "name" .}}               // вставка одного шаблона в другой (например, header или footer)
{{/* комментарий */}}               // комментарий, который не попадет в итоговый HTML

net/http
http.NewServeMux()             // создает чистый маршрутизатор без лишних путей
http.ListenAndServe(":8080", m) // запуск сервера на конкретном порту
http.Handle("/p", handler)     // регистрация обработчика (можно передать middleware)
http.StripPrefix("/v1/", h)    // удаляет префикс пути перед передачей в другой обработчик
http.TimeoutHandler(h, d, msg) // обертка: если обработчик h длится дольше d, вернет msg
http.ServeFile(w, r, "path")   // отдает файл с диска (для картинок/стилей)

net/http (Client)
client := &http.Client{Timeout: 5*time.Second} // создание защищенного клиента
client.Do(req)                 // выполнение запроса с настройками (заголовки, таймауты)
http.NewRequest(m, u, body)    // создание запроса (метод, URL, тело)
req.Header.Set("K", "V")       // установка заголовков (авторизация, Content-Type)
resp.Body.Close()              // закрытие тела ответа (утечки памяти недопустимы!)

encoding/json
json.NewDecoder(r.Body).Decode(&v) // читает JSON из входящего потока запроса
json.NewEncoder(w).Encode(v)       // пишет JSON в исходящий поток ответа
json.Marshal(v)                    // превращает структуру в []byte (строку JSON)
json.Unmarshal(data, &v)           // парсит []byte в структуру Go

context
context.Background()           // база для всех контекстов (обычно корень приложения)
context.WithTimeout(ctx, d)    // создает контекст с лимитом времени (убьет запрос через d)
context.WithCancel(ctx)        // создает контекст, который можно отменить принудительно
ctx.Done()                     // канал, сигнализирующий о завершении работы (успех или отмена)
ctx.Err()                      // узнает, почему контекст завершился (ошибка или таймаут)

sync
var mu sync.Mutex              // мьютекс для защиты данных от одновременной записи
mu.Lock()                      // захват доступа к данным
mu.Unlock()                    // освобождение доступа (обязательно через defer)
var wg sync.WaitGroup          // счетчик для ожидания завершения нескольких горутин
wg.Add(1)                      // добавляет задачу в список ожидания
wg.Done()                      // отмечает задачу как выполненную
wg.Wait()                      // блокирует код, пока все задачи из группы не завершатся

os
os.Getenv("PORT")              // чтение конфигурации из переменных окружения
os.Args                        // доступ к аргументам командной строки (например, адрес БД)
os.Exit(1)                     // аварийная остановка программы

log
log.Println("message")         // вывод обычного лога с временной меткой
log.Fatalf("error: %v", err)   // логирует ошибку и вызывает os.Exit(1)

database/sql
sql.Open(driverName, dataSourceName) // инициализирует пул соединений с БД для указанного драйвера (соединение не проверяется сразу)
db.Ping()                            // проверяет актуальность соединения с базой данных, выполняя реальный пинг
db.Close()                           // закрывает пул соединений, освобождая все ресурсы и завершая фоновые воркеры пула
db.Exec(query, args...)              // выполняет SQL-запрос, не возвращающий строк (INSERT, UPDATE, DELETE), возвращает Result
db.Query(query, args...)             // выполняет SQL-запрос, возвращающий набор строк (*sql.Rows) для итерации
db.QueryRow(query, args...)          // выполняет SQL-запрос, ожидающий максимум одну строку (*sql.Row)
db.Prepare(query)                    // создает предварительно скомпилированный SQL-запрос (*sql.Stmt) для повторного использования
db.Begin()                           // начинает новую транзакцию (*sql.Tx) с изоляцией по умолчанию
db.SetMaxOpenConns(n)                // ограничивает максимальное количество одновременно открытых соединений с БД
db.SetMaxIdleConns(n)                // ограничивает максимальное количество простаивающих соединений в пуле
db.SetConnMaxLifetime(d)             // задает максимальное время жизни соединения перед его принудительным закрытием
db.SetConnMaxIdleTime(d)             // задает максимальное время, которое соединение может простаивать в пуле
db.Stats()                           // возвращает структуру sql.DBStats с текущими метриками пула соединений

sql.Result
result.LastInsertId()                // возвращает целочисленный ID последней вставленной записи (если поддерживается БД)
result.RowsAffected()                // возвращает количество строк, затронутых последним запросом (UPDATE/DELETE/INSERT)

sql.Rows
rows.Next()                          // перемещает курсор к следующей строке результата, возвращает false по окончании
rows.Scan(dest...)                   // копирует колонки текущей строки в переданные указатели переменных
rows.Close()                         // закрывает курсор строк и возвращает соединение обратно в пул (критично для избежания утечек)
rows.Err()                           // возвращает ошибку, которая могла возникнуть во время итерации циклом Next()
rows.Columns()                       // возвращает срез строк с именами всех колонок в результате запроса
rows.NextResultSet()                 // переходит к следующему набору результатов (для хранимых процедур и множественных SELECT)

sql.Row
row.Scan(dest...)                    // сканирует единственную полученную строку в переменные; вернет sql.ErrNoRows, если ничего не найдено

sql.Tx
tx.Commit()                          // фиксирует (сохраняет) все изменения транзакции в базу данных
tx.Rollback()                        // отменяет все изменения текущей транзакции при возникновении ошибки
tx.Stmt(stmt)                        // привязывает подготовленный statement к конкретной транзакции
tx.Exec(query, args...)              // выполняет невозвратный запрос внутри контекста транзакции
tx.Query(query, args...)             // выполняет SELECT-запрос внутри контекста транзакции
tx.QueryRow(query, args...)          // выполняет запрос одной строки внутри контекста транзакции

sql.Stmt
stmt.Close()                         // закрывает подготовленный SQL-запрос и освобождает ресурсы на сервере БД
stmt.Exec(args...)                   // выполняет сохраненный подготовленный запрос с передачей аргументов
stmt.Query(args...)                  // выполняет сохраненный SELECT-запрос с аргументами
stmt.QueryRow(args...)               // выполняет сохраненный запрос одной строки с аргументами

sql.NamedArg / sql.Named
sql.Named("name", value)             // создает поименованный параметр для безопасной подстановки в SQL-запрос по имени ключа

sql.ErrNoRows
sql.ErrNoRows                        // стандартизированная ошибка, возвращаемая QueryRow, если в таблице нет подходящих строк
